{
  "queries": [
    "nonlinear models robustly outperform logistic regression sensor classification data classification dataset",
    "nonlinear models robustly outperform logistic regression sensor classification data openml huggingface dataset",
    "sensor classification dataset",
    "tabular classification benchmark",
    "One public sensor dataset (e.g., UCI HAR, EEG Eye State) with at least 1000 samples, binary or multiclass target, and standard train/test split.",
    "A nonlinear model can robustly outperform logistic regression across repeated seeds on a public sensor classification dataset."
  ],
  "required_properties": [
    "One public sensor dataset (e.g., UCI HAR, EEG Eye State) with at least 1000 samples, binary or multiclass target, and standard train/test split."
  ],
  "preferred_sources": [
    "huggingface",
    "openml"
  ],
  "rejection_criteria": [
    "no plausible supervised target",
    "too few rows or minority class too small",
    "features cannot be materialized into a safe local table"
  ],
  "rationale": "Dataset search plan derived from selected hypothesis `H1`."
}